version: '2'

networks:
  app-tier:
    driver: bridge

services:
  myscore:
    image: fdsfsrw12/my_image_score
    container_name: myscore
    ports:
      - 8777:5001
    volumes:
      - ./Scores.txt:/app/Scores.txt       
    networks:
      - app-tier
